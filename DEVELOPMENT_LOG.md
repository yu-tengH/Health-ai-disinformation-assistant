# 開發日誌：主要技術挑戰與解法

記錄本專案開發過程中遇到的幾個具代表性的技術問題，包含排查過程與根本原因，作為 README 的技術細節補充。

---

## 問題 1：向量檢索排序不準確——Chroma 預設距離計算方式不適合語意比對

**症狀**：使用者輸入與「蘋果山楂茶」案例高度相關的問題，檢索結果卻將該案例排在第 3 名，反而是主題無關的案例排名更前。

**排查過程**：先將 `TOP_K` 暫時調大到等於總案例數，觀察完整排序與距離數值，確認並非程式邏輯錯誤，而是排序結果本身不合理。

**根因**：Chroma 建立 collection 時，若未指定距離計算方式，預設使用 L2（歐幾里得）距離，這種算法對向量的長度較敏感；而文字嵌入模型的語意相似度，通常更適合用 cosine（餘弦）相似度衡量，因為它只看向量的方向、不受長度影響。

**解法**：在 `create_collection()` 時加入 `metadata={"hnsw:space": "cosine"}`，明確指定使用 cosine 距離。因為距離計算方式屬於 collection 建立時就固定的底層設定，資料庫需要重新建立才能套用。

---

## 問題 2：`mlx_lm` 匯入卡死，`Ctrl+C` 無法中斷

**症狀**：`import mlx_lm` 或 `from mlx_lm import load, generate` 執行後長時間無反應，CPU 使用率維持在 0%，`Ctrl+C` 也無法中斷執行。

**排查過程**：
1. 用活動監視器確認 CPU 使用率，發現確實是 0%（代表程式在等待某個資源，而非真的在運算）
2. 完整的 Traceback 顯示卡在 `importlib._bootstrap_external` 的 `get_data`（讀取檔案內容）這一步
3. 檢查專案路徑，發現整個專案（含 `venv`）都放在「桌面」資料夾底下

**根因**：Mac 的「iCloud 雲端硬碟 - 桌面與文件夾」同步功能，會將桌面資料夾內容同步到雲端，必要時本機只留占位符。`mlx_lm` 依賴的套件（如 `transformers`、`torch`、`sympy`）包含大量檔案，當 Python 嘗試逐一讀取這些檔案時，若剛好觸發雲端重新下載，會造成長時間無回應，且此過程不佔用 CPU，符合觀察到的症狀。

**解法**：將整個專案資料夾搬離桌面（例如搬到 `~/dev/`），脫離 iCloud 同步監控範圍；因虛擬環境內部路徑與新位置不符，需刪除舊 `venv` 並重新建立、重新安裝套件。

---

## 問題 3：Streamlit 網頁畫面完全空白，且終端機無任何輸出

**症狀**：`streamlit run app.py` 執行後伺服器正常啟動（顯示 Local URL），但瀏覽器畫面完全空白，終端機也沒有印出程式內的任何 `print` 訊息。

**排查過程**：
1. 逐步排除連接埠問題、瀏覽器快取問題、殘留程序問題
2. 用純 Python 直接呼叫 `load_models()`，確認函式本身完全正常、能成功載入模型
3. 回頭比對 `app.py` 完整程式碼，發現檔案最底部缺少 `if __name__ == "__main__": main()`

**根因**：`main()` 函式雖然被定義，但從未被呼叫。這與一般 Python 腳本不同——Streamlit 執行 `app.py` 時，是把整份檔案當作腳本從頭到尾執行一次，不會自動尋找或呼叫任何函式；若進入點沒有被觸發，畫面渲染邏輯自然完全不會執行。

**解法**：在檔案最底部補上 `if __name__ == "__main__": main()`，明確觸發主邏輯執行。

---

## 問題 4：Streamlit 呼叫 MLX 生成時報錯 `RuntimeError: There is no Stream(cpu, 0) in current thread`

**症狀**：網頁介面輸入問題並送出後，生成階段報錯；但同樣的模型呼叫邏輯，在純 Python 互動模式下完全正常，可重複執行不出錯。

**排查過程**：
1. 先嘗試用 `mx.stream(mx.default_device())` 包裝生成呼叫，無效，錯誤位置完全相同
2. 嘗試建立專屬 `ThreadPoolExecutor(max_workers=1)`，將模型載入與生成都丟進同一執行緒池執行，仍然報同樣的錯
3. 進一步檢視後發現，執行緒池物件本身是寫在模組層級（檔案最外層），而 Streamlit 每次使用者互動都會重新執行整份腳本，導致執行緒池物件本身也被重新建立，模型當初載入時所在的執行緒與生成時使用的執行緒其實並非同一個

**根因**：MLX 的底層運算資源通道（Stream）綁定在初始化當下所在的執行緒；Streamlit 為了保持介面流暢，會將程式邏輯派送到執行緒執行，且每次重新執行腳本都可能是不同的執行緒環境，若沒有明確保證模型載入與生成使用同一條執行緒，就會找不到對應的資源通道。

**解法**：將 `ThreadPoolExecutor` 的建立本身也用 `@st.cache_resource` 包裝，確保該執行緒池物件只被真正建立一次；之後模型載入（`load()`）與每次生成（`generate()`）都透過 `get_mlx_executor().submit(...).result()` 呼叫，確保兩者永遠在同一條背景執行緒中執行。

---

## 問題 5：爬蟲抓取的 CSS class 名稱不穩定，無法重複使用

**症狀**：檢視事實查核中心網站原始碼時，發現標題等元素所在的 `class` 名稱包含隨機字串（如 `kb-dynamic-html-id-38510_5a54c2-26`），且每篇文章都不同，無法用固定 class 名稱撰寫通用的擷取邏輯。

**排查過程**：比對多篇文章的原始碼，發現雖然大部分 class 名稱不穩定，但有兩類例外：文章網址固定遵循 `/fact-check-reports/文章代稱/` 的規律；分類與查核結果的 class（如 `fact-check-report-type-health`、`fact-check-report-classification-incorrect`）則是固定、可預期的字串，不含隨機成分。

**解法**：改用「網址規律比對」取代「CSS class 選取」來抓取文章連結，並用固定的分類 class 取出主題與查核結果，避開不穩定的隨機 class，大幅提升爬蟲的穩健性與可重複使用性。
